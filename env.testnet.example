# Testnet Environment Configuration
# Copy to .env for testnet trading (Binance Futures Testnet)

# Environment name
ENVIRONMENT=testnet

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Test interval in seconds (1 = 1-second candles, 3600 = 1-hour candles)
# Testnet uses 1-hour candles for realistic testing
TEST_INTERVAL=3600

# Enable trace logging: Generates detailed CSV files in reports/ folder for strategy auditing
# true = Enable trace logs, false = Disable (default)
ENABLE_TRACE=false

# ===== NOTES =====
# - Configure Binance testnet API keys in: gateways/binance/vault/binance_keys
# - Get testnet keys from: https://testnet.binancefuture.com
# - Uses FAKE money (no financial risk)

